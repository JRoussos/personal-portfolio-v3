import variables from '@styles/abstracts/_.module.scss'

/**
 * CSS Module `:export` map from `_.module.scss`.
 * Keys match the export identifiers (no Sass `$` prefix).
 * @type {{
 *   BACKGROUND_WHITE: string,
 *   BACKGROUND_DARK: string,
 * }}
 */
export const SASS_VARIABLES = variables;
